# Root Environment Variables

# JWT
JWT_SECRET=your_jwt_secret_key_change_in_production

# Database (if using external production DB)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/homefinance

# Deployment
DEPLOY_ENV=staging
